<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= locals.title ? title : 'Login System' %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Arima:wght@100..700&family=Lilita+One&family=Playfair+Display:ital,wght@0,400..900;1,400..900&family=Rubik+Wet+Paint&display=swap" rel="stylesheet">
    <style>
        body {
            background-color: black;/* Set your background image */
            background-size: cover; /* Cover the entire viewport */
            font-family: 'Arial', sans-serif; /* Change font style to Arial */
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        #login {
    width: 100%;
    max-width: 400px;
    padding: 30px 20px; /* Increased padding for more vertical space */
    min-height: 450px; /* Set a minimum height to ensure enough room */
    background: darkgreen;
    border-radius: 10px;
    display: flex;
    flex-direction: column;
    align-items: center; /* Center-aligns content */
    justify-content: space-between; /* Ensures spacing between content and buttons */
}

        .title {
            color: black; /* White title text */
            font-family: "Playfair Display", serif;
            text-align: center;
            margin-bottom: 2rem;
        }
        .alert-success {
            display: none;
            /* Adjust visibility as needed */
            background: black;
        }
        .form-control {
            background: black; /*Semi-transparent white input background */
            color: #fff; /* White text */
            font-weight: bolder;
            border: 1px solid white; /* Semi-transparent white border */
            margin-bottom: 10px;
            transition: border-color 0.15s ease-in-out, box-shadow 0.15s ease-in-out;
        }
        .form-control:focus {
            border-color: black; /* Blue border on focus */
            box-shadow: 0 0 0 0.25rem rgba(37, 117, 252, 0.25); /* Blue shadow on focus */
        }
        .text-danger {
            color: darkred; /* Red error message text */
        }
        .btn-success {
            width: 100%;
            background-color: black; 
            border-color: whitesmoke; 
            transition: background-color 0.3s ease, color 0.3s ease;
        }
        .btn-success:hover {
            background-color: darkgreen; 
            border-color: beige; 
        }
        .frm {
            text-align: center;
            margin-top: 1rem;
        }
        a {
            color: #2575fc; /* Blue link text */
            text-decoration: none;
        }
        a:hover {
            text-decoration: underline; /* Underline on hover */
        }
    </style>
</head>
<body>

<div class="text-center" id="login">
    <div class="container">
        <div class="title">
            <h2 class="font-weight-bold">Login System</h2>
        </div>
        <% if (message) { %>
            <div class="alert alert-danger text-center" role="alert" style="display: block; color: darkred;">
                <%= message %>
            </div>
        <% } %>
        <div class="alert alert-success text-center" role="alert">
            <!-- Success message display area -->
             
        </div>

        <form action="/route/login" method="POST" onsubmit="return validateForm()">
            <div class="form-group">
                <input type="email" class="form-control" placeholder="Email" name="email" id="email">
                <div id="emailError" class="text-danger"></div>
            </div>
          
            <div class="form-group">
                <input type="password" class="form-control" placeholder="Password" name="password" id="password">
                <div id="passwordError" class="text-danger"></div>
            </div>

            <div class="frm">
                <button type="submit" class="btn btn-success rounded-pill">Submit</button>
                <br>
                
             <!-- "Create an account" link styled as a button -->
<a href="/route/signup" style="
display: inline-block;
margin-top: 35px;
padding: 12px 24px;
background-color: #006400;
color: white;
font-weight: bolder;
font-family: Arial, sans-serif;
text-align: center;
text-decoration: none;
border-radius: 4px;
transition: background-color 0.3s, transform 0.2s;
">
Create an account
</a>

<script>
// Adding hover effect for the "Create an account" button
const createAccountLink = document.querySelector('a[href="/route/signup"]');

createAccountLink.addEventListener('mouseover', () => {
    createAccountLink.style.backgroundColor = 'black';
    createAccountLink.style.transform = 'scale(1.05)'; // Slightly enlarges on hover
});

createAccountLink.addEventListener('mouseout', () => {
    createAccountLink.style.backgroundColor = '#006400';
    createAccountLink.style.transform = 'scale(1)'; // Returns to original size
});
</script>
   
            </div>
        </form>
    </div>
</div>

<script>
    function validateForm() {
        var email = document.getElementById('email').value;
        var password = document.getElementById('password').value;
        var emailError = document.getElementById('emailError');
        var passwordError = document.getElementById('passwordError');

        // Reset previous error messages
        emailError.innerHTML = "";
        passwordError.innerHTML = "";

        // Validate email
        if (email.trim() === "") {
            emailError.innerHTML = "Email cannot be empty";
            return false;
        }

        // Validate password
        if (password.trim() === "") {
            passwordError.innerHTML = "Password cannot be empty";
            return false;
        }

        return true; // Allow form submission
    }
</script>

<%- include('footer') -%>

</body>
</html>
